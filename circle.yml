machine:
  node:
    version: 0.11.8
    
dependencies:
pre:
  - npm install gulp
 override:
    - npm install -g gulp
    - npm install
test:
  override:
    - gulp buildtask
    
  post:
    - echo "$CIRCLE_ARTIFACTS"
    
    

